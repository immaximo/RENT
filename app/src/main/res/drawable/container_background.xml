<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Set background color -->

    <corners
        android:topLeftRadius="16dp"
        android:topRightRadius="16dp"
        android:bottomLeftRadius="0dp"
        android:bottomRightRadius="0dp" /> <!-- Only top corners are rounded -->

    <stroke
        android:color="@android:color/darker_gray"
        android:width="1dp" /> <!-- Border color and width -->

    <padding
        android:left="16dp"
        android:right="16dp" /> <!-- Padding inside the container -->
</shape>
